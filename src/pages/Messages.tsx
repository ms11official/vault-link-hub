import DefaultCategoryPage from "@/components/DefaultCategoryPage";

const Messages = () => (
  <DefaultCategoryPage
    title="Messages"
    itemType="message"
    categoryType="messages"
    emptyMessage="No messages yet. Add your first message!"
  />
);

export default Messages;
