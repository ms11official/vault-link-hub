import DefaultCategoryPage from "@/components/DefaultCategoryPage";

const WebUrls = () => (
  <DefaultCategoryPage
    title="Web URLs"
    itemType="weburl"
    categoryType="weburls"
    emptyMessage="No web URLs yet. Add your first URL!"
  />
);

export default WebUrls;
