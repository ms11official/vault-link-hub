import DefaultCategoryPage from "@/components/DefaultCategoryPage";

const Emails = () => (
  <DefaultCategoryPage
    title="Emails"
    itemType="email"
    categoryType="emails"
    emptyMessage="No emails yet. Add your first email!"
  />
);

export default Emails;
