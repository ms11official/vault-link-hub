import DefaultCategoryPage from "@/components/DefaultCategoryPage";

const Passwords = () => (
  <DefaultCategoryPage
    title="Passwords"
    itemType="password"
    categoryType="passwords"
    emptyMessage="No passwords yet. Add your first password!"
  />
);

export default Passwords;
